<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3><%= @admin.name %>から技工物の完成通知が届きました。</h3>
    <ul>
      <li><strong>患者名：</strong><%= @order.patient_name %></li>
      <li><strong>注文内容：</strong><%= @order.content.gsub!(/[\[\]\"]/, "").gsub!(",","") if @order.content.present? %><%= @order.other_text if @order.other_text.present? %></li>
      <li><strong>試適日：</strong><%= l(@order.first_try) if @order.first_try.present? %></li>
      <li><strong>完成日：</strong><%= l(@order.complete_day) if @order.complete_day.present? %></li>
    </ul>
  </body>
</html>